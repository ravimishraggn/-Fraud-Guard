# Development overrides — applied automatically by `docker compose up`.
services:
  backend:
    environment:
      LOG_LEVEL: DEBUG

  celery_worker:
    environment:
      LOG_LEVEL: DEBUG

  frontend:
    environment:
      WATCHPACK_POLLING: "true"
